apiVersion: v1
kind: Service
metadata:
  name: "{{ .Release.Name }}-game"
  labels:
    app: "{{ .Chart.Name }}"
    chart: "{{ .Chart.Name }}-{{ .Chart.Version }}"
    release: "{{ .Release.Name }}"
spec:
  type: NodePort
  ports:
    - port: {{ .Values.config.port }}
      targetPort: game
      protocol: UDP
      name: game
      {{- if .Values.config.nodePort.game }}
      nodePort: {{ .Values.config.nodePort.game }}
      {{- end }}
    - port: {{ .Values.config.queryPort }}
      targetPort: query
      protocol: UDP
      name: query
      {{- if .Values.config.nodePort.query }}
      nodePort: {{ .Values.config.nodePort.query }}
      {{- end }}
  selector:
    app: "{{ .Chart.Name }}"
    release: "{{ .Release.Name }}"
